# ─── .env template — copy to .env and fill in. NEVER commit .env. ───
# App runtime/deployment variables only.

# ─── Application API keys (examples — adapt/remove per project) ───
# ALCHEMY_API_KEY=
# DUNE_API_KEY=
