# Outils Python de la CI, épinglés.
#
# POURQUOI CE FICHIER PLUTÔT QU'UN `pip install zizmor==X` DANS LE YAML :
# un bot d'update ne lit JAMAIS une commande shell. Une version épinglée dans un `run:` n'est
# surveillée par PERSONNE — elle pourrit sur place, et un scanner de sécurité figé finit par
# rater ce qu'il est censé trouver. Ici, Renovate le suit NATIVEMENT (manager `pip_requirements`
# auto-détecté), sans une ligne de configuration custom.
#
# Épinglés à la version EXACTE (`==`) : une CI doit être reproductible, et un bump de scanner
# peut faire apparaître des findings — cela se regarde dans une PR, pas un matin au hasard.
zizmor==1.27.0
semgrep==1.169.0
