<!--
  README modèle — DOUBLE CIBLE, sans choisir :
    (1) un dev qui clone / forke / contribue → install, run, structure, contribution ;
    (2) une vitrine honnête qui donne envie sans survendre.
  Léché, concis, ZÉRO salade. Bilingue : anglais d'abord (défaut), français ensuite, séparés par `---`.
  Ton : jamais de 2e personne — « the user / l'utilisateur », pas « you / vous ».
  Supprimer ces commentaires une fois rempli.
-->

# <project> — <one-line subtitle that says what it is>

<One honest hook, framed around the real problem the user has. No empty superlatives.>

<!-- If the tool is third-party / unofficial / touches funds, keep a disclaimer here:
> ⚠️ **Unofficial, third-party tool.** Not affiliated with <X>. Reads public data only, never custodies funds.
> Provided as-is, no warranty.
-->

<!-- Screenshots, light + dark theme (drop the block if none):
<p align="center"><picture>
  <source media="(prefers-color-scheme: dark)" srcset="docs/img/hero-dark.png">
  <img src="docs/img/hero-light.png" width="700" alt="<project>">
</picture></p>
-->

## Why this exists
<The problem, and why this solves it better than the obvious alternative. The "why" before the "how".>

## Quick start
```bash
git clone https://github.com/<owner>/<repo>.git
cd <repo>
<run command>
```

## Structure
- `<dir>/` — <…>

## Development
<Branches, tests, contributing — or point to CONTRIBUTING.md.>

<!--
  Limitations / Non-goals — REPLACES a ROADMAP file. State what the tool does NOT do, and
  what it will never do. Two payoffs: it stops the wrong user from adopting it, and it closes
  feature requests before they are opened. A roadmap ages badly and reads as a promise;
  a non-goal is a decision, and it stays true.
  Be concrete: "no write access, ever" beats "security-focused".
-->
## Limitations / Non-goals
- **<Limitation>** — <the honest consequence for the user, not a euphemism.>
- **Not a goal: <thing it will never do>** — <why. A deliberate boundary, not a missing feature.>

## License
[MIT](LICENSE)

---

# <projet> — <sous-titre d'une ligne qui dit ce que c'est> (français)

<Une accroche honnête, orientée le problème réel de l'utilisateur. Pas de survente.>

## Pourquoi cet outil
<Le problème, et pourquoi cet outil le résout mieux que l'alternative évidente.>

## Démarrage rapide
```bash
git clone https://github.com/<owner>/<repo>.git
cd <repo>
<commande de lancement>
```

## Structure
- `<dossier>/` — <…>

## Développement
<Branches, tests, contribution — ou pointer vers CONTRIBUTING.md.>

## Limites / Ce que l'outil ne fera pas
- **<Limite>** — <la conséquence réelle pour l'utilisateur, sans euphémisme.>
- **Hors périmètre : <ce que l'outil ne fera jamais>** — <pourquoi. Une frontière assumée, pas un manque.>

## Licence
[MIT](LICENSE)
