# secrets & local config
.env
.env.local
.envrc
.claude/

# deps
node_modules/

# build artifacts
dist/
build/
.tsbuildinfo
.vite/

# runtime data
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# pinned binaries check.sh downloads to keep local runs aligned with the CI
.ci-tools/

# CI Python tooling, subtracted from the dependency scan — but VERSIONED (AGENTS.md, "Do not touch")
/requirements-ci.txt

# misc
.DS_Store
*.log
*.bak
*.bak-*
