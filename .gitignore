# Secrets & local config. The leading `/` ANCHORS at the root: without it, the homonymous
# templates under `templates/repo/` are ignored too — and why they are force-added: AGENTS.md.
/.env
/.envrc
/.claude/

.DS_Store
*.log
*.bak
*.bak-*

# Pinned binaries that `check.sh` downloads to align local with CI.
/.ci-tools/

.playwright-mcp/
